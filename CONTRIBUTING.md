# Contributing to SelfOS

Thank you for your interest in contributing to SelfOS! We welcome contributions from the community to help make SelfOS a better personal operating system.

## 🚀 Getting Started

1. **Fork the Repository**: Create your own copy of the project.
2. **Clone the Fork**: `git clone https://github.com/YOUR_USERNAME/SelfOS.git`
3. **Install Dependencies**: `npm install`
4. **Create a Branch**: `git checkout -b feature/your-feature-name`

## 🛠️ Development Workflow

- **Development Server**: Run `npm run dev` to start the SvelteKit development server.
- **Linting & Checking**: Run `npm run check` to ensure TypeScript and Svelte components are valid.
- **Commit Style**: Use descriptive commit messages (e.g., `feat: add new widget`, `fix: resolve layout issue`).

## 📜 Coding Guidelines

- **Svelte 5**: Use Svelte 5 Runes (`$state`, `$derived`, `$props`) for state management.
- **Tailwind CSS**: Use Tailwind classes for styling. Avoid inline styles or custom CSS unless necessary.
- **JSDoc**: Add JSDoc comments to all export functions in utility files and major methods in stores.
- **Accessibility**: Ensure UI components are accessible (ARIA roles, keyboard support).

## 📬 Submitting Changes

1. **Push to your fork**: `git push origin feature/your-feature-name`
2. **Open a Pull Request**: Provide a clear description of the changes and link any related issues.

---

*Together, let's build a more mindful digital life.*
